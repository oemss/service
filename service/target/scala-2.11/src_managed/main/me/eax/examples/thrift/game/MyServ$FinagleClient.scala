/**
 * Generated by Scrooge
 *   version: 3.16.3
 *   rev: b8593c83072d94fc44feaa8d97940b9266d84ed0
 *   built at: 20140806-054445
 */
package me.eax.examples.thrift.game

import com.twitter.finagle.{SourcedException, Service => FinagleService}
import com.twitter.finagle.stats.{NullStatsReceiver, StatsReceiver}
import com.twitter.finagle.thrift.ThriftClientRequest
import com.twitter.scrooge.{ThriftStruct, ThriftStructCodec}
import com.twitter.util.{Future, Return, Throw}
import java.nio.ByteBuffer
import java.util.Arrays
import org.apache.thrift.protocol._
import org.apache.thrift.TApplicationException
import org.apache.thrift.transport.{TMemoryBuffer, TMemoryInputTransport}
import scala.collection.{Map, Set}


@javax.annotation.Generated(value = Array("com.twitter.scrooge.Compiler"))
class MyServ$FinagleClient(
  val service: FinagleService[ThriftClientRequest, Array[Byte]],
  val protocolFactory: TProtocolFactory = new TBinaryProtocol.Factory,
  val serviceName: String = "",
  stats: StatsReceiver = NullStatsReceiver
) extends MyServ[Future] {
  import MyServ._

  protected def encodeRequest(name: String, args: ThriftStruct) = {
    val buf = new TMemoryBuffer(512)
    val oprot = protocolFactory.getProtocol(buf)

    oprot.writeMessageBegin(new TMessage(name, TMessageType.CALL, 0))
    args.write(oprot)
    oprot.writeMessageEnd()

    val bytes = Arrays.copyOfRange(buf.getArray, 0, buf.length)
    new ThriftClientRequest(bytes, false)
  }

  protected def decodeResponse[T <: ThriftStruct](resBytes: Array[Byte], codec: ThriftStructCodec[T]) = {
    val iprot = protocolFactory.getProtocol(new TMemoryInputTransport(resBytes))
    val msg = iprot.readMessageBegin()
    try {
      if (msg.`type` == TMessageType.EXCEPTION) {
        val exception = TApplicationException.read(iprot) match {
          case sourced: SourcedException =>
            if (serviceName != "") sourced.serviceName = serviceName
            sourced
          case e => e
        }
        throw exception
      } else {
        codec.decode(iprot)
      }
    } finally {
      iprot.readMessageEnd()
    }
  }

  protected def missingResult(name: String) = {
    new TApplicationException(
      TApplicationException.MISSING_RESULT,
      name + " failed: unknown result"
    )
  }

  protected def setServiceName(ex: Exception): Exception =
    if (this.serviceName == "") ex
    else {
      ex match {
        case se: SourcedException =>
          se.serviceName = this.serviceName
          se
        case _ => ex
      }
    }

  // ----- end boilerplate.

  private[this] val scopedStats = if (serviceName != "") stats.scope(serviceName) else stats
  private[this] object __stats_add {
    val RequestsCounter = scopedStats.scope("add").counter("requests")
    val SuccessCounter = scopedStats.scope("add").counter("success")
    val FailuresCounter = scopedStats.scope("add").counter("failures")
    val FailuresScope = scopedStats.scope("add").scope("failures")
  }
  
  def add(idR: String, idT: String): Future[Unit] = {
    __stats_add.RequestsCounter.incr()
    this.service(encodeRequest("add", add$args(idR, idT))) flatMap { response =>
      val result = decodeResponse(response, add$result)
      val exception: Future[Nothing] =
        null
  
      if (exception != null) exception else Future.Done
    } respond {
      case Return(_) =>
        __stats_add.SuccessCounter.incr()
      case Throw(ex) =>
        __stats_add.FailuresCounter.incr()
        __stats_add.FailuresScope.counter(ex.getClass.getName).incr()
    }
  }
  private[this] object __stats_delete {
    val RequestsCounter = scopedStats.scope("delete").counter("requests")
    val SuccessCounter = scopedStats.scope("delete").counter("success")
    val FailuresCounter = scopedStats.scope("delete").counter("failures")
    val FailuresScope = scopedStats.scope("delete").scope("failures")
  }
  
  def delete(idR: String, idT: String): Future[Unit] = {
    __stats_delete.RequestsCounter.incr()
    this.service(encodeRequest("delete", delete$args(idR, idT))) flatMap { response =>
      val result = decodeResponse(response, delete$result)
      val exception: Future[Nothing] =
        null
  
      if (exception != null) exception else Future.Done
    } respond {
      case Return(_) =>
        __stats_delete.SuccessCounter.incr()
      case Throw(ex) =>
        __stats_delete.FailuresCounter.incr()
        __stats_delete.FailuresScope.counter(ex.getClass.getName).incr()
    }
  }
  private[this] object __stats_listT {
    val RequestsCounter = scopedStats.scope("listT").counter("requests")
    val SuccessCounter = scopedStats.scope("listT").counter("success")
    val FailuresCounter = scopedStats.scope("listT").counter("failures")
    val FailuresScope = scopedStats.scope("listT").scope("failures")
  }
  
  def listT(idR: String): Future[Unit] = {
    __stats_listT.RequestsCounter.incr()
    this.service(encodeRequest("listT", listT$args(idR))) flatMap { response =>
      val result = decodeResponse(response, listT$result)
      val exception: Future[Nothing] =
        null
  
      if (exception != null) exception else Future.Done
    } respond {
      case Return(_) =>
        __stats_listT.SuccessCounter.incr()
      case Throw(ex) =>
        __stats_listT.FailuresCounter.incr()
        __stats_listT.FailuresScope.counter(ex.getClass.getName).incr()
    }
  }
  private[this] object __stats_listR {
    val RequestsCounter = scopedStats.scope("listR").counter("requests")
    val SuccessCounter = scopedStats.scope("listR").counter("success")
    val FailuresCounter = scopedStats.scope("listR").counter("failures")
    val FailuresScope = scopedStats.scope("listR").scope("failures")
  }
  
  def listR(lstT: Seq[String] = Seq[String]()): Future[Unit] = {
    __stats_listR.RequestsCounter.incr()
    this.service(encodeRequest("listR", listR$args(lstT))) flatMap { response =>
      val result = decodeResponse(response, listR$result)
      val exception: Future[Nothing] =
        null
  
      if (exception != null) exception else Future.Done
    } respond {
      case Return(_) =>
        __stats_listR.SuccessCounter.incr()
      case Throw(ex) =>
        __stats_listR.FailuresCounter.incr()
        __stats_listR.FailuresScope.counter(ex.getClass.getName).incr()
    }
  }
  private[this] object __stats_put {
    val RequestsCounter = scopedStats.scope("put").counter("requests")
    val SuccessCounter = scopedStats.scope("put").counter("success")
    val FailuresCounter = scopedStats.scope("put").counter("failures")
    val FailuresScope = scopedStats.scope("put").scope("failures")
  }
  
  def put(where: Short): Future[Unit] = {
    __stats_put.RequestsCounter.incr()
    this.service(encodeRequest("put", put$args(where))) flatMap { response =>
      val result = decodeResponse(response, put$result)
      val exception: Future[Nothing] =
        null
  
      if (exception != null) exception else Future.Done
    } respond {
      case Return(_) =>
        __stats_put.SuccessCounter.incr()
      case Throw(ex) =>
        __stats_put.FailuresCounter.incr()
        __stats_put.FailuresScope.counter(ex.getClass.getName).incr()
    }
  }
}